/*
Código que recebe um inteiro e diz se esse inteiro, que é uma idade, é adulto, idoso ou jovem.
*/

//inicio do algoritomo
algoritmo idade;

função teste(idade: inteiro, nome: literal) : inteiro
início
	idade := idade + 100;
	nome := "jader";	
fim
fim-função

//variáveis
variáveis
	idade,a1a,a: inteiro;
	nome: literal;
fim-variáveis



//inicio do programa
início
	idade := 1 + teste(a,nome);
	nome := "jader";
	a1a := media(idade,a);
	a1a := maximo(a1a,a);
	a1a := 1 + minimo(a1a,a);
	imprima(a);
	imprima_ln(a);
	idade := 1 + teste(a,nome);
	leia(a);
	leia_ln(a);
fim
