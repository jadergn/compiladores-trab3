/*
Código que recebe um inteiro e diz se esse inteiro, que é uma idade, é adulto, idoso ou jovem.
*/

//inicio do algoritomo
algoritmo idade;

função teste(idade: inteiro, nome: literal) : literal
início
	idade := 100;
	nome := "jader";	
fim
fim-função

//variáveis
variáveis
	idade,a1a,a: inteiro;
	nome: literal;
fim-variáveis

//inicio do programa
início
	idade := teste(a,a1a,a1a);
	nome := "jader";
	a := 0 + "jader";
fim
