/*
Código que recebe um inteiro e diz se esse inteiro, que é uma idade, é adulto, idoso ou jovem.
*/

//inicio do algoritomo
algoritmo idade;

função teste(idade: inteiro, nome: literal) : inteiro
início
	idade := idade + 100;
	nome := "jader";	
fim
fim-função

//variáveis
variáveis
	idade,a1a,a: inteiro;
	nome: literal;
	r: real;
fim-variáveis



//inicio do programa
início
	//idade := 1 + teste(a,a1a);
	nome := "jader";
	teste(nome, nome);
	//r := r + nome;
fim
