algoritmo fatorial_recursivo;

variáveis
    fat : inteiro;
    x : inteiro;
fim-variáveis

função fatorial(x1: inteiro, y1:inteiro, z1:real) : inteiro
início
    retorne 1;
fim

função fatorial2(x2: inteiro) : real
início
    retorne 1.0;
fim

função fatorial3(x3: inteiro, z3:real) : caractere
início
    retorne '1';
fim

início
    //imprima("Digite um número:");
    //fat := leia();
    imprima("Fatorial de ", fat, " é igual a ", fatorial(fat));
fim

