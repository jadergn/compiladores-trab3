algoritmo fatorial_recursivo;


variáveis
    fat : inteiro;
    x : inteiro;
    nome : literal;
    c : caractere;
    r: real;
fim-variáveis


função primeira_funcao() : inteiro
variáveis
    a : inteiro;
    b : inteiro;
    f : inteiro;
fim-variáveis
início
    a := 1;
    b := 2;
    f := 3;
    retorne a + b + f;
fim


função primeira_funcao() : inteiro
início
    retorne 1;
fim

início
    primeira_funcao();
    x := primeira_funcao();
    x := 1 + fat + x;
    r := r + 1;
    nome := r + 1;
//  r := r + c;
//  x := 1 + c + nome;
fim

